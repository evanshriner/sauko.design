declare module '@emotion/react' {
  export interface Theme {
    colors: {
      primaryText: string;
      primaryBackground: string;
      defaultText: string;
      defaultTextFilter: string;
    };
  }
}
