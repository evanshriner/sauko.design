import styled from '@emotion/styled';
import NeonText from '../../styles/NeonText';

const Logo = styled(NeonText)(() => ({
  fontFamily: 'Orbit',
  whiteSpace: 'nowrap',
  cursor: 'pointer',
}));

export { Logo };
